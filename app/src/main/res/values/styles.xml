<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="SplashScreenProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/splash_screen_progress_bar</item>
        <item name="android:minHeight">20dp</item> <!-- Altura mínima de la barra de progreso -->
        <item name="android:maxHeight">60dp</item> <!-- Altura máxima de la barra de progreso -->
    </style>
</resources>